<div class="container">
    <h3>Vagas disponivel</h3>

    <%= button_to root_path do%>
        <span class="glyphicon glyphicon-search"></span>
    <% end %>

    <div class="row">
        <div class="col-sm-10">
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>VAGAS</th>
                    </tr>
                </thead>
                <tbody>
                    <%if @job_vacancies.empty?%>
                        <tr>
                            <td>Não existem vagas abertas no momento</td>
                        </tr>
                    <%else%>
                        <% @job_vacancies.each do |vacancy|%>
                            <tr>
                                <%= render partial:"vacancy", locals: {vacancy:vacancy}%>
                            </tr>
                        <%end%>
                    <%end%>           
                </tbody>
            </table>
        </div>
    </div>

</div>